/**
 * @expx/painel — ponto de entrada do pacote.
 *
 * O painel é somente leitura: nunca escreve nos arquivos do projeto observado
 * e nunca executa comando algum. Ver docs/expx-panel/ORQUESTRADOR.md.
 */

/** Versão do contrato expx-schema que este painel sabe ler. */
export const VERSAO_SCHEMA_SUPORTADA = 1;
